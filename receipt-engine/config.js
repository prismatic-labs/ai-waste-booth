/* config.js — swap these URLs when Track B services are configured */

const CONFIG = {
  UPGRADE_URL:      "https://placeholder-stripe.example.com",   // Stripe £15 Payment Link
  TEARDOWN_URL:     "https://placeholder-calendly.example.com", // Calendly 20-min teardown
  INTAKE_FORM_URL:  "https://placeholder-intake.example.com",   // Tally intake form (post-payment)
  CENSUS_SIGNUP_URL:"https://placeholder-census.example.com",   // Email capture fallback form
  CENSUS_ENDPOINT:  "",  // leave empty — set when Track C census endpoint is live
};
